name: VKD3D-Proton Forza Workarounds Nightlies

# Builds VKD3D-Proton from the upstream `forza-workarounds` branch
# (https://github.com/HansKristian-Work/vkd3d-proton/tree/forza-workarounds)
# instead of master. That branch carries Hans-Kristian's in-flight Forza Horizon
# game-specific workarounds plus the descriptor-heap / workgraphs / DLSS heap
# infrastructure those workarounds sit on top of. The FH6 fixes are gated by
# exact executable-name match on `forzahorizon6.exe`, so other games launched
# with this build are unaffected by the FH6 quirks themselves — but the shared
# infrastructure deltas (resource.c / state.c / config flag additions) do apply
# unconditionally, so treat this as an experimental channel alongside the
# master-tracking standalone nightly (vkd3dProtons-standalone-nightly.yml).
#
# Run manually when you want a fresh build of the forza-workarounds HEAD.
# Releases land at tags `vkd3d-proton-forza-nightly-<timestamp>` and are marked
# pre-release + non-latest so they do NOT displace the master nightly's
# `nightly-latest` pointer.

on:
  workflow_dispatch:

jobs:
  # ==================================================================
  # JOB 1: VKD3D-Proton (Standard) — built from forza-workarounds
  # ==================================================================
  build-vkd3d:
    name: Build VKD3D-Proton (Standard, forza-workarounds)
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      commit: ${{ steps.version.outputs.commit }}
    steps:
      - name: Install Dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends \
            ca-certificates curl xz-utils jq git zip \
            build-essential python3 pkg-config zstd \
            meson ninja-build wget unzip

      - name: Setup GLSLANG
        run: |
          wget -q https://github.com/KhronosGroup/glslang/releases/download/master-tot/glslang-master-linux-Release.zip
          unzip -q glslang-master-linux-Release.zip -d glslang
          echo "$PWD/glslang/bin" >> $GITHUB_PATH

      - name: Setup Toolchain
        run: |
          LLVM_MINGW_TAG="20251104"
          TOOLCHAIN_DIR="/opt/llvm-mingw"
          wget -q "https://github.com/mstorsjo/llvm-mingw/releases/download/$LLVM_MINGW_TAG/llvm-mingw-$LLVM_MINGW_TAG-ucrt-ubuntu-22.04-x86_64.tar.xz" -O llvm.tar.xz
          sudo rm -rf "$TOOLCHAIN_DIR" && sudo mkdir -p "$TOOLCHAIN_DIR"
          sudo tar -C "$TOOLCHAIN_DIR" --strip-components=1 -xJf llvm.tar.xz
          echo "$TOOLCHAIN_DIR/bin" >> $GITHUB_PATH

      - name: Build VKD3D (forza-workarounds branch)
        id: version
        run: |
          git clone --recursive --branch forza-workarounds https://github.com/HansKristian-Work/vkd3d-proton src
          cd src
          COMMIT_HASH=$(git rev-parse --short HEAD)
          LATEST_TAG=$(git describe --tags --abbrev=0 | sed 's/^v//')
          VERSION_NAME="${LATEST_TAG}-forza-${COMMIT_HASH}"
          echo "VERSION_NAME=${VERSION_NAME}" >> $GITHUB_ENV
          echo "version=${VERSION_NAME}" >> $GITHUB_OUTPUT
          echo "commit=${COMMIT_HASH}" >> $GITHUB_OUTPUT
          cd ..

          cat <<EOF > cross-x64.txt
          [binaries]
          c = '/opt/llvm-mingw/bin/x86_64-w64-mingw32-clang'
          cpp = '/opt/llvm-mingw/bin/x86_64-w64-mingw32-clang++'
          ar = '/opt/llvm-mingw/bin/llvm-ar'
          strip = '/opt/llvm-mingw/bin/llvm-strip'
          windres = '/opt/llvm-mingw/bin/x86_64-w64-mingw32-windres'
          widl = '/opt/llvm-mingw/bin/x86_64-w64-mingw32-widl'
          pkg-config = 'pkg-config'
          exe_wrapper = ['wine']
          [host_machine]
          system = 'windows'
          cpu_family = 'x86_64'
          cpu = 'x86_64'
          endian = 'little'
          EOF

          cat <<EOF > cross-x86.txt
          [binaries]
          c = '/opt/llvm-mingw/bin/i686-w64-mingw32-clang'
          cpp = '/opt/llvm-mingw/bin/i686-w64-mingw32-clang++'
          ar = '/opt/llvm-mingw/bin/llvm-ar'
          strip = '/opt/llvm-mingw/bin/llvm-strip'
          windres = '/opt/llvm-mingw/bin/i686-w64-mingw32-windres'
          widl = '/opt/llvm-mingw/bin/i686-w64-mingw32-widl'
          pkg-config = 'pkg-config'
          exe_wrapper = ['wine']
          [host_machine]
          system = 'windows'
          cpu_family = 'x86'
          cpu = 'i686'
          endian = 'little'
          EOF

          cd src
          meson setup build-x64 --cross-file ../cross-x64.txt -Dbuildtype=release -Dstrip=true
          ninja -C build-x64 -j 2
          meson setup build-x86 --cross-file ../cross-x86.txt -Dbuildtype=release -Dstrip=true
          ninja -C build-x86 -j 2

      - name: Package
        run: |
          rm -rf final_package && mkdir -p final_package/system32 final_package/syswow64

          find src/build-x64 -name "d3d12.dll" -exec cp -L {} final_package/system32/ \;
          find src/build-x64 -name "d3d12core.dll" -exec cp -L {} final_package/system32/ \;
          find src/build-x86 -name "d3d12.dll" -exec cp -L {} final_package/syswow64/ \;
          find src/build-x86 -name "d3d12core.dll" -exec cp -L {} final_package/syswow64/ \;

          cat <<EOF > final_package/profile.json
          {
            "type": "VKD3D",
            "versionName": "${{ env.VERSION_NAME }}",
            "versionCode": 1,
            "description": "${{ steps.version.outputs.commit }} (forza-workarounds branch)",
            "files": [
              {"source": "system32/d3d12.dll", "target": "\${system32}/d3d12.dll"},
              {"source": "system32/d3d12core.dll", "target": "\${system32}/d3d12core.dll"},
              {"source": "syswow64/d3d12.dll", "target": "\${syswow64}/d3d12.dll"},
              {"source": "syswow64/d3d12core.dll", "target": "\${syswow64}/d3d12core.dll"}
            ]
          }
          EOF

          cd final_package

          # Create WCP
          tar -cJf ../VKD3D-Proton-${{ env.VERSION_NAME }}.wcp *

          # Create ZIP
          zip -r ../VKD3D-Proton-${{ env.VERSION_NAME }}.zip *

      - uses: actions/upload-artifact@v4
        with:
          name: vkd3d-forza-standard
          path: |
            *.wcp
            *.zip

  # ==================================================================
  # JOB 2: VKD3D-Proton (ARM64EC) — built from forza-workarounds
  # ==================================================================
  build-vkd3d-arm64ec:
    name: Build VKD3D-Proton (ARM64EC, forza-workarounds)
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      commit: ${{ steps.version.outputs.commit }}
    steps:
      - name: Install Dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends \
            ca-certificates curl xz-utils jq git zip \
            build-essential python3 pkg-config zstd \
            meson ninja-build wget unzip

      - name: Setup GLSLANG
        run: |
          wget -q https://github.com/KhronosGroup/glslang/releases/download/master-tot/glslang-master-linux-Release.zip
          unzip -q glslang-master-linux-Release.zip -d glslang
          echo "$PWD/glslang/bin" >> $GITHUB_PATH

      - name: Setup Toolchain
        run: |
          LLVM_MINGW_TAG="20251104"
          TOOLCHAIN_DIR="/opt/llvm-mingw"
          wget -q "https://github.com/mstorsjo/llvm-mingw/releases/download/$LLVM_MINGW_TAG/llvm-mingw-$LLVM_MINGW_TAG-ucrt-ubuntu-22.04-x86_64.tar.xz" -O llvm.tar.xz
          sudo rm -rf "$TOOLCHAIN_DIR" && sudo mkdir -p "$TOOLCHAIN_DIR"
          sudo tar -C "$TOOLCHAIN_DIR" --strip-components=1 -xJf llvm.tar.xz
          echo "$TOOLCHAIN_DIR/bin" >> $GITHUB_PATH

      - name: Build ARM64EC (forza-workarounds branch)
        id: version
        run: |
          git clone --recursive --branch forza-workarounds https://github.com/HansKristian-Work/vkd3d-proton src
          cd src
          COMMIT_HASH=$(git rev-parse --short HEAD)
          LATEST_TAG=$(git describe --tags --abbrev=0 | sed 's/^v//')
          VERSION_NAME="arm64ec-${LATEST_TAG}-forza-${COMMIT_HASH}"
          echo "VERSION_NAME=${VERSION_NAME}" >> $GITHUB_ENV
          echo "version=${VERSION_NAME}" >> $GITHUB_OUTPUT
          echo "commit=${COMMIT_HASH}" >> $GITHUB_OUTPUT
          cd ..

          cat <<EOF > arm64ec.txt
          [binaries]
          c = 'arm64ec-w64-mingw32-clang'
          cpp = 'arm64ec-w64-mingw32-clang++'
          ar = 'llvm-ar'
          strip = 'llvm-strip'
          windres = 'arm64ec-w64-mingw32-windres'
          widl = 'arm64ec-w64-mingw32-widl'
          pkg-config = 'pkg-config'
          [host_machine]
          system = 'windows'
          cpu_family = 'aarch64'
          cpu = 'aarch64'
          endian = 'little'
          EOF

          cat <<EOF > x86.txt
          [binaries]
          c = 'i686-w64-mingw32-clang'
          cpp = 'i686-w64-mingw32-clang++'
          ar = 'llvm-ar'
          strip = 'llvm-strip'
          windres = 'i686-w64-mingw32-windres'
          widl = 'i686-w64-mingw32-widl'
          pkg-config = 'pkg-config'
          [host_machine]
          system = 'windows'
          cpu_family = 'x86'
          cpu = 'i686'
          endian = 'little'
          EOF

          cd src
          meson setup build-ec --cross-file ../arm64ec.txt -Dbuildtype=release -Dstrip=true
          ninja -C build-ec -j 2
          meson setup build-x86 --cross-file ../x86.txt -Dbuildtype=release -Dstrip=true
          ninja -C build-x86 -j 2

      - name: Package
        run: |
          rm -rf final_package && mkdir -p final_package/system32 final_package/syswow64

          find src/build-ec -name "d3d12.dll" -exec cp -L {} final_package/system32/ \;
          find src/build-ec -name "d3d12core.dll" -exec cp -L {} final_package/system32/ \;
          find src/build-x86 -name "d3d12.dll" -exec cp -L {} final_package/syswow64/ \;
          find src/build-x86 -name "d3d12core.dll" -exec cp -L {} final_package/syswow64/ \;

          llvm-strip --strip-all final_package/system32/*.dll
          llvm-strip --strip-all final_package/syswow64/*.dll

          cat <<EOF > final_package/profile.json
          {
            "type": "VKD3D",
            "versionName": "${{ env.VERSION_NAME }}",
            "versionCode": 1,
            "description": "${{ steps.version.outputs.commit }} (forza-workarounds branch)",
            "files": [
              {"source": "system32/d3d12.dll", "target": "\${system32}/d3d12.dll"},
              {"source": "system32/d3d12core.dll", "target": "\${system32}/d3d12core.dll"},
              {"source": "syswow64/d3d12.dll", "target": "\${syswow64}/d3d12.dll"},
              {"source": "syswow64/d3d12core.dll", "target": "\${syswow64}/d3d12core.dll"}
            ]
          }
          EOF

          cd final_package

          # WCP
          tar --zstd -cf ../VKD3D-Proton-${{ env.VERSION_NAME }}.wcp *

          # ZIP
          zip -r ../VKD3D-Proton-${{ env.VERSION_NAME }}.zip *

      - uses: actions/upload-artifact@v4
        with:
          name: vkd3d-forza-arm64ec
          path: |
            *.wcp
            *.zip

  # ==================================================================
  # JOB 3: Release
  # ==================================================================
  create-release:
    name: Create Forza Nightly Release
    runs-on: ubuntu-latest
    needs: [build-vkd3d, build-vkd3d-arm64ec]
    permissions:
      contents: write
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts
          merge-multiple: true

      - name: Set Release Variables
        run: |
          TIMESTAMP=$(date +'%Y%m%d-%H%M%S')
          READABLE_DATE=$(date +'%Y-%m-%d %H:%M:%S UTC')

          echo "TAG_NAME=vkd3d-proton-forza-nightly-$TIMESTAMP" >> $GITHUB_ENV
          echo "RELEASE_NAME=VKD3D-Proton Forza Workarounds Nightly Build - $READABLE_DATE" >> $GITHUB_ENV

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG_NAME }}
          name: ${{ env.RELEASE_NAME }}
          prerelease: true
          make_latest: false
          body: |
            ### 🏁 VKD3D-Proton Forza Workarounds Nightly Build: ${{ env.TAG_NAME }}

            Built from the upstream **`forza-workarounds`** branch
            (https://github.com/HansKristian-Work/vkd3d-proton/tree/forza-workarounds) — Hans-Kristian's
            in-flight branch carrying Forza Horizon game-specific workarounds plus the descriptor-heap /
            workgraphs / DLSS heap infrastructure those workarounds sit on top of.

            | Component | Source / Commit |
            | :--- | :--- |
            | **VKD3D-Proton (Std, forza-workarounds)** | [${{ needs.build-vkd3d.outputs.version }}](https://github.com/HansKristian-Work/vkd3d-proton/commit/${{ needs.build-vkd3d.outputs.commit }}) |
            | **VKD3D-Proton (ARM64EC, forza-workarounds)** | [${{ needs.build-vkd3d-arm64ec.outputs.version }}](https://github.com/HansKristian-Work/vkd3d-proton/commit/${{ needs.build-vkd3d-arm64ec.outputs.commit }}) |

            ### 📦 Files Included
            1. **VKD3D-Proton (Std)**: `VKD3D-Proton-${{ needs.build-vkd3d.outputs.version }}.wcp` (and `.zip`)
            2. **VKD3D-Proton (ARM64EC)**: `VKD3D-Proton-${{ needs.build-vkd3d-arm64ec.outputs.version }}.wcp` (and `.zip`)

            ### ⚠️ Read me

            - **For Forza Horizon 6**, the FH6 workarounds inside the build are gated by exact executable-name
              match on `forzahorizon6.exe`, so they auto-apply when FH6 launches. Other games in the same
              container are unaffected by the FH6-specific quirks.
            - The underlying infrastructure deltas (descriptor heap rework, workgraphs-on-heap, DLSS heap
              path, dxil-spirv submodule bump) DO apply unconditionally — treat this build as **experimental**
              for everything other than FH6.
            - Released as `prerelease: true` and `make_latest: false` so the **master-tracking nightly**
              ([vkd3dProtons-standalone-nightly.yml](../../.github/workflows/vkd3dProtons-standalone-nightly.yml))
              remains your default channel.

            **The .wcp files are packaged for use with Winlator**
            **The .zip files are packaged for manual injection with Gamehub**
          files: |
            ./artifacts/*.wcp
            ./artifacts/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
